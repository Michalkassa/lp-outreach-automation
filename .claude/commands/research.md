Build or refresh the context file for the LP in $ARGUMENTS at
/lps/<lp-slug>.md using /lps/_TEMPLATE.md.

1. Web search the company: type, AUM, mandate in THEIR words, private
   credit / real assets exposure, commitments and public statements from
   the last 18 months. Source everything. No padding, no guessing —
   unverified items get [VERIFY].
2. Find 2-3 potential contact PEOPLE: name, exact title, why them,
   source. Rank: primary owns private markets / credit allocations;
   for small family offices the principal is fine. Never guess email
   addresses — if not public, write the likely pattern and mark FIND.
3. Rank 3 bridge candidates (the fact for sentence two) and pick the
   category label per templates/voice.md.
4. Add/update the row in tracker.csv (stage=research).
5. End with 3 lines: fit score, best bridge, best contact.
